We now need to extract key metrics from an Excel workbook (“deal-2642-export (1).xlsx”) and save them to the company information database. The file contains a single sheet named “Income Statement” whose first column is the row label (e.g., “Revenue”, “Gross Profit”, “Operating Income”, etc.) and whose subsequent columns represent monthly periods (YYYY‑MM) from 2021‑04 to 2025‑03. Please implement the following extraction logic:

1. **Read the workbook** using a library such as `openpyxl` or `pandas`. Identify the sheet named “Income Statement”.

2. **Identify the latest month column** by parsing the header row (row 1). The last non‑empty header (e.g., “2025-03”) represents the most recent period, which we will refer to as `latest_month`.

3. **Extract the following line items for `latest_month`:**
   - **Revenue**: value from the row labelled “Revenue” and column `latest_month`.
   - **Net Revenues (Astrotalk India / International / E‑Commerce / Other Experimental Apps)**: values from the corresponding rows (labels begin with “    Net Revenues (… )”) and column `latest_month`. These breakdowns should be stored for detailed reporting but can be aggregated if a single revenue figure is needed.
   - **COGS**: value from the row labelled “COGS” and column `latest_month` (note that expenses are stored as negative numbers).
   - **Gross Profit**: value from the row labelled “Gross Profit” and column `latest_month`.
   - **Sales And Marketing**: value from the row labelled “Sales And Marketing” and column `latest_month`.
   - **Other Opex**: value from the row labelled “Other Opex” and column `latest_month`.
   - **Operating Income**: value from the row labelled “Operating Income” and column `latest_month`.
   - **lifetime_gross_margin**: percentage from that row and column `latest_month`.
   - **lifetime_operating_margin**: percentage from that row and column `latest_month`.
   - **rule_of_40**, **magic_number**, **burn_multiple**, **margin_burn_multiple**: numbers from the corresponding rows and `latest_month`.

4. **Handle currency differences**:
   - Determine the currency unit of each value. If the workbook stores amounts in different currencies (e.g., some rows in INR and others in USD), infer the currency either from column metadata or from the context of the line item.
   - Choose a standard reporting currency, e.g. **USD**.  Apply a consistent conversion rate (for example, a fixed INR→USD rate pulled from configuration or an up‑to‑date FX service) to convert any INR amounts into USD before computing totals and ratios.
   - For transparency, also store the original value and its currency, alongside the converted value, so that the system can display the chosen currency while retaining traceability.

5. **Compute derived metrics**:
   - **Gross Margin** = `gross_profit / revenue` (be careful: COGS is negative in the sheet; gross profit is already calculated).
   - **Operating Margin** = `operating_income / revenue`.
   - **Revenue Growth (MoM)** = `(revenue_latest_month – revenue_prev_month) / revenue_prev_month`, where `revenue_prev_month` is from the column immediately before `latest_month`.
   - **Monthly Surplus** = `operating_income` (since this represents income after expenses).
   - Ensure that signs are preserved (expenses are negative). After currency conversion, convert these to percentages or millions-of-dollars as appropriate for display in the UI.

6. **Handle missing values**: if any row or cell is missing for `latest_month`, leave the corresponding field as `null` rather than defaulting to zero.  Do not multiply or scale values beyond what is present in the sheet.

7. **Save the extracted and computed metrics** into the company information database with the following field mappings:
   - `monthly_revenue` → Revenue (latest month) in USD
   - `monthly_surplus` → Operating Income (latest month) in USD
   - `gross_margin` → computed Gross Margin (%)
   - `operating_margin` → computed Operating Margin (%)
   - `revenue_growth_mom` → computed Revenue Growth (MoM)
   - `burn_multiple` → value from the “burn_multiple” row (latest month)
   - `rule_of_40` → value from “rule_of_40”
   - `magic_number` → value from “magic_number”
   - `margin_burn_multiple` → value from “margin_burn_multiple”
   - `lifetime_gross_margin`, `lifetime_operating_margin` → values from their respective rows
   - along with any auxiliary fields capturing both the original currency amount and the converted USD amount.

8. **Verify your results** by checking that:
   - The extracted revenue for 2025‑03 is ≈ 14 246 988.40 (≈ $14.2 M) after any currency conversion.
   - The gross margin equals (Revenue – COGS)/Revenue and matches the lifetime_gross_margin in the sheet (≈ 60 %).
   - The operating margin equals Operating Income / Revenue and is around 30 %.
   - The burn multiple matches the value reported in the sheet (around –0.7× as seen in the PDF) and is not forced to zero.

By following these steps—including the new currency‑standardization step—you will ensure that the Excel parser correctly reads the latest data, performs necessary currency conversions to a unified USD basis, computes relevant margins and growth metrics, and saves the results consistently in the company database without the scaling and mapping errors observed previously.
