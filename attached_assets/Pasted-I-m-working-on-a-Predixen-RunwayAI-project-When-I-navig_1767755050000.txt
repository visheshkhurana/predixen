I'm working on a Predixen/RunwayAI project. When I navigate to the "Data Input" section (URL `/data`), the app shows an error that says: "Did you forget to add the page to the router?":contentReference[oaicite:0]{index=0}. It looks like this page isn't wired up. I need help to:

1. **Add the Data Input page to the router:**
   - Define a route for `/data` or similar in the routing configuration (e.g., React Router, Next.js routing, or whatever framework is used).
   - Import the Data Input component and register it so that the page renders instead of the error card.

2. **Build the Data Input page:**
   - **Company Information**: fields for company name, description, founding date, stage (e.g., pre‑seed, seed, Series A), and industry/sector.
   - **Financial Metrics**: inputs for current revenue (monthly/annual), expenses (e.g., payroll, marketing, operating costs), cash on hand/runway, revenue growth rate, burn rate, and number of employees.
   - **Goals and Assumptions**: target runway length, growth scenarios (optimistic, conservative, worst‑case), and funding target/timeline.
   - Include labels, placeholders, and validation (e.g., prevent negative numbers, ensure required fields are filled).

3. **Integrate an OpenAI API–powered assistant:**
   - Add a text area or chat panel where users can ask questions like:
     - “What is our runway given our burn rate?”
     - “Find recent news about our company.”
     - “Suggest comparable companies in our industry.”
   - Use the OpenAI API to parse the user’s prompt and return a succinct answer, pulling in relevant data from the inputs and (when appropriate) combining it with general knowledge.
   - Optionally call a search API (or some web‑search function) to bring in up‑to‑date public data when the user asks for external information.

4. **User Experience:**
   - Organize the form into clear sections (Company Info, Financials, Goals, AI Assistant).
   - Provide sample prompts to guide users on what to ask the AI assistant.
   - Display status or validation messages so users know if fields are missing or invalid.

Can you help me set up the routing, create the form with these fields, and illustrate how to integrate the OpenAI API to answer user prompts and pull in relevant web data?
