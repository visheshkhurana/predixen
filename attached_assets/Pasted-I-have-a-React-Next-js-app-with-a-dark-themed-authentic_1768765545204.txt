I have a React/Next.js app with a dark‑themed authentication page. Right now the page shows a heading “Predixen Intelligence OS,” a short tagline, and two buttons/tabs for “Sign In” and “Create Account.” When the “Sign In” tab is active, it displays email and password fields and a “Sign In” button; there’s also a secondary “Try Demo Account” button. I want to make this screen look more professional and user‑friendly. Please implement the following changes:

1. **Header & Layout**
   - At the top of the auth card, include a strong, concise headline (e.g. “Welcome back to Predixen Intelligence OS”) and a one‑sentence subheading describing the benefit of signing in (e.g. “AI‑powered financial intelligence for startups”). Use typography hierarchy (larger font, weight) for the headline.
   - Centre the card on the page with balanced padding and rounded corners. On large screens, consider splitting the page into two columns: the left side with an illustrative graphic or gradient background and the right side with the auth card. On small screens, stack everything vertically.

2. **Tab Navigation**
   - Convert the current “Sign In” and “Create Account” buttons into a tabbed navigation bar. Style the active tab with a contrasting background or underline to clearly indicate which form is visible. Use accessible roles (`role="tablist"`, `role="tab"`).
   - Make the tabs take up the full width of the card, separated by subtle dividers.

3. **Form Fields**
   - Use floating labels (labels that act as placeholders and move above the input when focused or populated) for the email and password fields. This reduces clutter while keeping the labels visible.
   - Provide a password visibility toggle (eye icon) in the password field so users can show/hide their password.
   - For the create‑account form, initially ask only for essential information: email, password, and confirm password. Move any additional company details to a post‑signup onboarding step.
   - Add inline validation and helper text (e.g. “Minimum 8 characters, at least one number” for the password) and show error messages directly beneath the fields on invalid input.

4. **Authentication Options**
   - Above or below the tabs, add social login buttons for Google and GitHub using OAuth (e.g. “Continue with Google”). Style these as full‑width buttons with provider icons.
   - Keep the existing demo account option, but style it as a tertiary link button below the main form (e.g. small text “Try a demo account”).

5. **Call‑to‑Action Buttons**
   - Use a prominent, brand‑colored primary button for the form submission (“Sign In” or “Create Account” depending on the active tab). Ensure the button spans the width of the card with sufficient margin.
   - Place secondary actions like “Forgot password?” or “Try demo account” below the main button in smaller text.

6. **Accessibility & Responsiveness**
   - Ensure all interactive elements (tabs, inputs, buttons) are keyboard‑navigable and have appropriate `aria` attributes.
   - Provide focus outlines for accessibility.
   - Make the page fully responsive: the card should shrink appropriately on mobile without forcing horizontal scrolling, and on larger screens the graphic/illustration should complement the form.

7. **Styling**
   - Use the existing Tailwind CSS or component library in the project. Define consistent spacing, rounded corners, and shadow for the card.
   - Maintain the dark‑theme aesthetic but use lighter backgrounds for inputs and subtle accent colors for buttons and active tabs to improve contrast and readability.

Please refactor or recreate the authentication components to incorporate these changes. Ensure that the logic for sign‑in, sign‑up, demo login, and tab switching continues to work as before, but with the improved layout and styling.
