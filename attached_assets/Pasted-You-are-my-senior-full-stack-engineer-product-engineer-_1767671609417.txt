You are my senior full-stack engineer + product engineer + product designer. In the existing Replit project “Fund Flow” (Predixen Intelligence OS), add the following **new features and UX upgrades** exactly as specified. Do NOT delete any existing code. Make changes in small checkpoints and keep the app working end-to-end.

=====================================================================
0) NON-NEGOTIABLE: INVESTOR MODE FEATURE FLAG (HIDE ONLY)
=========================================================

* Introduce env flag: `FEATURE_INVESTOR_MODE` default **false**.
* When false:

  1. Hide Investor nav items and routes in frontend (do not remove investor pages/components).
  2. Block all backend `/investor/*` endpoints with HTTP **403**.
  3. If user tries to access an investor route, show a “Feature not enabled” page.
* Keep investor code in repo intact.

Implementation:

* Backend: `server/core/config.py` reads env, exposes `settings.FEATURE_INVESTOR_MODE`.
* Frontend: `client/src/config/features.ts` reads `import.meta.env.VITE_FEATURE_INVESTOR_MODE`, default false.
* Add a shared helper `isInvestorEnabled()` in both frontend and backend.
* Add tests or a simple manual verification note in README.

=====================================================================

1. UX/IA: MAKE “TRUTH → SIMULATION → DECISION” THE PRIMARY FLOW
   =====================================================================
   Global layout requirements (implement exactly):

* AppShell with Sidebar (collapsible) + Topbar + main canvas.
* Default theme: **dark**, light supported.
* Accent: electric blue/teal.
* 2xl rounded cards, subtle borders, soft shadows, lots of whitespace.
* Typography hierarchy:

  * Page title: `text-2xl font-semibold`
  * Section headings: `text-lg font-medium`
  * Body: `text-sm text-muted-foreground`
* Microinteractions:

  * hover states on cards/buttons
  * loading skeletons (never blank)
  * smooth transitions (`transition`, `duration-200`)

Topbar must include:

* Stepper: “Truth → Simulation → Decision” with current step highlighted.
* Data Confidence pill (0–100) color:

  * <60 red, 60–80 amber, >80 green
* Last scan/run timestamp
* User menu (logout)

Sidebar menu (Founder mode primary):

* Overview
* Truth Scan
* Scenarios
* Decisions
* Copilot
* Settings
* Investor items exist but hidden when feature flag false.

Routing:

* Ensure routes are under `client/src/routes.tsx`.
* Founder routes under `/founder/*`.
* Investor routes under `/investor/*` but gated.

=====================================================================
2) WEEKLY FOUNDER “EXECUTIVE SNAPSHOT” (OVERVIEW PAGE)
======================================================

Implement /founder/overview with:
A) Top section: Executive Snapshot KPIs (2 rows x 3 MetricCards):

* Runway (p50 months) + p10/p90
* Net Burn (monthly)
* Revenue Growth (MoM)
* Gross Margin %
* Retention (12m revenue or logo)
* Concentration (Top5 share)
  Each MetricCard shows:
* Value
* Benchmark badge (Above p50 / Below p25 if benchmarks available)
* Delta vs last scan (if previous scan exists)
* Optional sparkline

B) “What Predixen recommends right now”:

* Show top 3 decision actions (if decisions exist)
* Each compact card shows:

  * title
  * delta survival_18m
  * delta runway_p50
* CTA: “Open Decisions”

C) “Ask Copilot”:

* One-line input + suggested prompts:

  * “How do I extend runway by 6 months?”
  * “What’s the riskiest assumption?”
  * “What should I do if fundraise slips 3 months?”

Data fetching:

* Use React Query hooks to fetch:

  * latest truth scan
  * latest decisions
  * context pack summary

Empty states:

* If no company: show onboarding CTA.
* If no truth scan: show “Run Truth Scan” CTA.

=====================================================================
3) TRUTH SCAN PAGE: TRUST + BENCHMARKS + FLAGS (Termina-lite)
=============================================================

Implement /founder/truth-scan layout:

Section A: Scores

* Two large score cards:

  * Quality of Growth Index (0–100)
  * Data Confidence Score (0–100)
* Tooltip “What this means”

Section B: Benchmarks

* For each key metric, render BenchmarkBar:

  * p25/p50/p75 markers + company value dot
  * show direction (higher is better / lower is better)
* If industry/stage missing: fallback to general_saas / unknown_stage.

Section C: Flags

* List top flags sorted by severity:

  * severity badge (high/med/low)
  * title + 1-line description
  * metric chips
* Click opens drawer:

  * What it means
  * Why it matters
  * Recommended next data to upload / checks to run

Truth Scan algorithm must compute (from uploaded data if available else manual baseline):

* revenue growth (MoM, QoQ)
* gross margin %, operating margin %
* net burn (monthly), burn multiple
* cash balance + runway p10/p50/p90 months (if cash known)
* concentration: top1/top5 revenue share (from transactions)
* retention: logo + revenue retention 6m/12m (if customer + transactions exist)
* unit econ: LTV, CAC, LTV/CAC, payback (CAC manual if missing)

Scores:

1. Data Confidence Score 0-100:

* coverage of required fields (weight 40)
* missingness (weight 20)
* consistency checks (weight 20):

  * sum(txn) approx revenue if both exist (tolerance 5–10%)
* outlier checks (weight 10)
* time coverage (weight 10)

2. Quality of Growth Index 0-100:

* growth + stability (30)
* retention stability (20)
* margin health (15)
* burn efficiency (15)
* concentration penalty (-10)
* confidence penalty (- up to 20 based on low confidence)

Store outputs in `truth_scans.outputs_json` including:

* metrics dict
* scores dict
* benchmarks dict (p25/p50/p75 + position)
* flags list (severity,title,detail,metrics_involved)
* created_at

Backend endpoints (must exist and work):

* POST /companies/{id}/truth/run
* GET /companies/{id}/truth/latest

=====================================================================
4) SCENARIOS PAGE: TEMPLATE-DRIVEN BUILDER + KNOBS (NO CONFUSING SLIDERS)
=========================================================================

Implement /founder/scenarios:

Top: Scenario templates (cards):

* Extend runway
* Grow faster
* Prep fundraise
* Pricing change
* Cost restructure
  Each template pre-fills knobs (deterministic presets).

Scenario editor (2-column):
Left: ScenarioKnobPanel

* Pricing slider (-10% to +15%)
* Burn reduction slider (0 to 35%)
* Growth uplift slider (-5% to +20%)
* Hiring plan widget:

  * add role name + count + start month + cost/hire
  * shows incremental monthly payroll
* Fundraise widget:

  * month 1–12
  * amount
  * probability (0–1), default 1.0
    Important:
* Sliders must default to baseline-derived sensible values (NOT 100).
* Show formatted labels and tooltips.
* Always show current knob values (e.g. “+5% pricing”).

Right: Instant preview panel:

* estimated net burn change
* rough runway p50 preview (if last sim exists) else show “Run sim to compute”

CTAs:

* Run Simulation
* Save Scenario

Backend:

* POST /companies/{id}/scenarios
* GET /companies/{id}/scenarios

Scenario storage:

* scenarios.inputs_json must include baseline fields from truth scan + scenario knobs.

=====================================================================
5) SIMULATION: MONTE CARLO + SURVIVAL CURVES + BANDS (24 months)
================================================================

Implement backend simulation algorithm exactly:

Inputs:

* baseline from truth scan:

  * starting_cash
  * monthly_revenue
  * gross_margin
  * opex
  * payroll
  * other_costs
  * baseline_growth_mean (monthly)
* knobs:

  * pricing_change_pct
  * growth_uplift_pct
  * burn_reduction_pct
  * hiring plan (month + payroll add)
  * fundraise (month + amount + probability)
* stochastic:

  * growth_rate_t ~ Normal(mean=baseline_growth + uplift, sigma configurable)
  * gross margin varies with sigma
* cashflow:
  cash[t+1] = cash[t] + (rev[t]*gm - opex - payroll - other_costs) + fundraise_if_any

Outputs (store in simulation_runs.outputs_json):

* runway distribution (months to cash<=0)
* survival probabilities at 6/12/18/24
* p10/p50/p90 bands for revenue, burn, cash
* survival curve points

Endpoints:

* POST /scenarios/{id}/simulate
* GET /scenarios/{id}/simulation/latest

Frontend:

* After running simulation, redirect to Decisions page with scenario selected.
* Render charts:

  * SurvivalCurveChart
  * BandsChart (cash, revenue, burn)
  * RunwayDistribution (p10/p50/p90 clearly labeled)

Performance:

* Default n_sims=1000, horizon=24
* Must complete in a few seconds.

=====================================================================
6) DECISIONS: TOP 3 RANKED ACTIONS (DETERMINISTIC, NO RANDOM)
=============================================================

Implement /founder/decisions:

Top section: “Recommended actions (ranked)”

* Show 3 large DecisionCards.
  Each DecisionCard must include:
* Title
* Why it wins (1–2 lines)
* Expected Impact:

  * delta survival_18m (%)
  * delta runway p50 (months)
* Risks & Assumptions (bullets)
* Buttons:

  * “Run validation scenario” (auto-applies delta, runs sim)
  * “Make this my plan” (store chosen strategy)

Second section: CompareTable

* baseline vs current vs top action scenarios:

  * survival_18m
  * runway_p50
  * revenue_18m median
  * burn_multiple
* highlight best cells

Third section: Charts

* Survival curve chart
* Bands chart
* Runway distribution chart

Decision engine rules (must be deterministic):

* Candidate action library in `server/decision/action_library.json`.
* Select 8–12 candidates deterministically based on truth metrics:

  * If confidence < 60: conservative only (burn cuts, fundraise, pricing small)
  * If runway_p50 < 6: include opex20/30, freeze hiring, fundraise M1/M3, bridge+burn10, burn20+raise
  * If concentration > 50%: deprioritize marketing push; prefer pricing/GM/burn
  * If retention 12m low: deprioritize marketing push; include GM improvement / product focus actions
* For each candidate:

  * generate scenario deltas
  * run simulation (cache identical)
  * compute:
    survival_score = P(survive 18m)
    runway_score = clamp(runway_p50/24, 0..1)
    growth_score = clamp(revenue18m_median/baseline_revenue18m,0..2)/2
    efficiency_score = map burn multiple improvement to 0..1
    risk_penalty deterministic:

    * if confidence<60 and bucket=growth => +0.15
    * if runway<9 and adds hires => +0.10
    * if burn_reduction_pct>25 => +0.05
    * if pricing_change_pct>=8 => +0.05 unless confidence>75 and retention strong
      total = 0.45*survival + 0.20*runway + 0.15*efficiency + 0.20*growth - risk_penalty
* Return top 3 with:
  title, rationale, expected impact vs baseline, risks, key assumption, next validation delta.

Backend endpoints:

* POST /simulation/{run_id}/decisions/generate
* GET /companies/{id}/decisions/latest

Store in `decisions.recommended_actions_json`.

=====================================================================
7) COPILOT: GROUNDED MULTI-AGENT + CONTEXT PANEL
================================================

Implement /founder/copilot:

Layout: split view

* Left: chat thread
* Right: context panel showing:

  * Data confidence
  * QoG score
  * current scenario knobs
  * latest survival/runway summary

Copilot behaviors:

* Every response MUST include:

  1. Answer
  2. Evidence metrics used (list of metric keys)
  3. Suggested next experiment (button)
* Provide quick buttons:

  * “Run what-if: pricing +5%”
  * “Run what-if: burn cut 20%”
  * “Run what-if: raise in 3 months”
    Clicking runs POST /companies/{id}/simulate and refreshes context.

Grounding enforcement (hard rule):

* Agents MUST only use numbers present in Context Pack.
* If missing: explicitly say missing and request minimal inputs.
* Never invent benchmarks.

Agents:

* Truth Analyst
* Simulation Engineer (calls simulate tool)
* Decision Strategist (calls decision compare if missing)
* CFO Planner
* Risk & Controls (guardrails)

Backend:

* GET /companies/{id}/context returns context pack:

  * company profile
  * latest truth scan summary
  * latest scenario + simulation summary
  * benchmark references used
  * available_metrics list (keys)
* POST /companies/{id}/simulate (scenario deltas, runs sim, stores run)
* POST /companies/{id}/decision/compare (select candidates, run sims, store decisions)

If the exact schema/prompts referenced earlier are NOT available in this chat, do NOT fabricate them:

* Create placeholder schema file and prompts with TODO,
* expose endpoints and UI working with safe stubs,
* and request me to paste exact contents later.

=====================================================================
8) INGESTION + MAPPING UI POLISH (TRUST FIRST)
==============================================

Onboarding Step 2/3 must be polished:

* Upload cards for each dataset type with file status & row count
* Mapping table:

  * left detected columns
  * right dropdown required field
  * auto-suggest fuzzy match
* Show “Missing fields” checklist
* Must block proceeding if essential financial fields missing (revenue + costs or cash)

Backend:

* store datasets metadata in DB
* parse immediately into DB tables
* track mapping metadata

=====================================================================
9) ENGINEERING REQUIREMENTS
===========================

* Use React Query for server state; Zustand only for UI session state (selected company, selected scenario).
* Use skeleton loaders; never show blank screens.
* Provide user-friendly errors.
* Keep app fast; Monte Carlo runs should complete quickly.
* Ensure formatting for currency and percent with tooltips.
* Maintain type safety end-to-end.

=====================================================================
10) DELIVERABLES
================

1. Working app end-to-end:
   Auth → onboarding → truth scan → scenario sim → decisions → copilot
2. Investor mode hidden but code present (verify feature flag works)
3. Short summary of new/modified files and what each does
4. Confirm all endpoints above exist and return expected payloads
