You are Replit Agent working inside the **Fund Flow** repo. Implement the following features end-to-end (DB + API + UI), with production-quality code and defensive error handling. Do NOT break existing flows. After each major change, run the app and verify the feature manually.

========================================================
0) FIRST: QUICK REPO ORIENTATION (DO THIS BEFORE CODING)
========================================================

1. Inspect current copilot stack:

   * client/src/pages/copilot.tsx (UI)
   * server API routes for copilot chat (search for /companies/{company_id}/chat)
   * company model / migrations (drizzle) and where CKB is stored
   * existing CKB schema / metadata_json usage
2. Identify:

   * where messages are stored (if at all)
   * where company metadata is stored (companies table metadata_json?)
   * current agent routing logic (router/cfo/market/strategy)

Deliverable: a short note (as a code comment in a new file docs/copilot_v2_plan.md) describing the current architecture + where you will add the new pieces.

========================================================

1. DATA MODEL: CKB v2 (MEMORY LAYERS) + DECISIONS + SCENARIOS
   ========================================================
   Add structured persistence for:
   A) Company Knowledge Base v2 layers:

   * facts: extracted business facts + financials
   * beliefs: assumptions user/agent made (with timestamps)
   * decisions: decision objects (see below)
   * outcomes: later results tied to decisions (optional now, but create schema)

B) Decision Objects (core):
Create table: company_decisions
Fields:

* id (uuid)
* company_id (fk)
* title (text)
* context (text)
* options_json (json)  // array of {name, description, pros, cons, cost, risk}
* recommendation_json (json) // {option, rationale, next_steps}
* status (text enum: proposed|approved|rejected|revisited)
* owner (text nullable)
* tags (text[] or json array)
* created_at, updated_at
* created_from_message_id (nullable, if you have messages)
* confidence (text enum: high|medium|low)
* sources_json (json) // citations, doc refs, web refs

C) Scenario Forking:
Create table: company_scenarios
Fields:

* id (uuid)
* company_id (fk)
* name (text)
* base_scenario_id (uuid nullable)
* assumptions_json (json) // e.g. {"raise_usd": 5000000, "cac_change_pct": 20}
* outputs_json (json) // computed outputs (runway, revenue impact, etc.)
* created_at, updated_at

Add migration(s) via Drizzle and ensure production db can migrate safely.

========================================================
2) BACKEND: ROUTER UPGRADES + NEW ENDPOINTS
===========================================

A) Extend /companies/:companyId/chat:

* Accept request:
  {
  "message": string,
  "mode": {
  "challenge": boolean,
  "investor_lens": "none" | "vc" | "growth_pe" | "crossover" | "strategic",
  "confidence_reporting": boolean
  },
  "scenario": {
  "scenario_id": string | null,
  "fork": boolean,
  "assumptions": object | null
  },
  "create_decision": boolean
  }

B) Router logic:

* If challenge mode is on, tell Strategy Agent to produce a "Counter-Case" section.
* If investor_lens is set, all agents should tailor outputs to that lens.
* If scenario.fork is true, create a new company_scenarios record, link base_scenario_id, store assumptions_json, and ask CFO Agent to compute outputs_json.

C) Decision Object creation:

* When create_decision=true OR router detects a "decision-worthy" request (e.g. user asks to choose between options), create a company_decisions record.
* Decision extraction rules:

  * Title must be short and specific.
  * Options must be explicit.
  * If the user didn’t provide options, generate 2–3 plausible options but label them as “Generated options”.
  * Store confidence and sources.
* Return decision_id in response.

D) Add endpoints:

* GET /companies/:companyId/decisions (list)
* POST /companies/:companyId/decisions (create)
* PATCH /companies/:companyId/decisions/:decisionId (update status/fields)
* GET /companies/:companyId/scenarios (list)
* POST /companies/:companyId/scenarios (create/fork)
* GET /companies/:companyId/ckb (return CKB v2 with layers)
* PATCH /companies/:companyId/ckb (merge new facts/beliefs/outcomes)

========================================================
3) AGENT OUTPUT CONTRACT (STRICT)
=================================

Update each agent (CFO, Market, Strategy) to return a structured payload:
{
"agent": "cfo|market|strategy",
"findings": string[],
"structured": object,
"assumptions_risks": string[],
"next_questions": string[]
}

Also include a top-level "confidence" field per agent.

Router merges them into the final response format:

* Executive Summary (≤5 bullets)
* Financials (table if available)
* Market & Customers (ICP + target customer archetypes + competitors)
* Strategy Options (2–4 options)
* Recommendation (ranked)
* Assumptions & Data Gaps
* Next Questions (≤5)

========================================================
4) MARKET AGENT: SALES-READY OUTPUTS
====================================

Add a "This Month Targets" section:

* 3 ICP segments
* For each segment:

  * buying trigger
  * who buys (titles)
  * suggested outbound angle (1–2 lines)
  * where to find them (channels)
* Do NOT claim a company is a customer.
* If you name target logos, label as "target prospects" and explain why.

========================================================
5) STRATEGY AGENT: CHALLENGE MODE + INVESTOR LENS
=================================================

A) Challenge Mode:

* Add "Counter-Case" section:

  * top 5 reasons this will fail
  * leading indicators to watch
  * mitigations
    B) Investor Lens:
* VC: focus on growth loops, PMF, scalability, retention
* Growth PE: efficiency, margins, payback, predictability
* Crossover: durability + category leadership signals
* Strategic: synergies, integration risk, defensibility

========================================================
6) UI: COPILOT PAGE UPGRADES
============================

In client/src/pages/copilot.tsx (or wherever):
A) Add toggles:

* Challenge Mode (toggle)
* Investor Lens (dropdown)
* Confidence Badges (toggle)
  B) Add "Create Decision" button:
* When clicked, send create_decision=true.
* Display returned decision_id with a link to a new Decisions page.
  C) Add Scenario controls:
* Select scenario (dropdown)
* Fork scenario (button)
* Add assumptions (simple key/value editor)
  D) Response rendering:
* Collapsible sections for each agent
* Confidence badges per section (High/Med/Low)
  E) Add “Next Actions” buttons below response:
* 3 suggestions returned by router (you must implement router to output next_actions: string[])

Create/Update pages:

* client/src/pages/decisions.tsx: list + detail view + status updates
* client/src/pages/scenarios.tsx: list + compare scenarios side-by-side

========================================================
7) EXPORTABLE MEMOS (MINIMUM VIABLE)
====================================

Add a server endpoint:
POST /companies/:companyId/memo
Body: { type: "board_memo" | "investor_memo", decision_id?: string, scenario_id?: string }
Returns: { markdown: string }

Generate a clean markdown memo with:

* header (company, date)
* executive summary
* key metrics
* decision/options
* recommendation
* risks/assumptions
  (Use the latest CKB + decision/scenario)

UI:

* Add "Export Memo" button in copilot + decisions pages.
* Show memo in a modal with copy button.

========================================================
8) TESTING & QA CHECKLIST (DO NOT SKIP)
=======================================

1. Run migrations successfully.
2. Create a company → open Copilot → ask a question → get response.
3. Toggle Challenge Mode → confirm Counter-Case appears.
4. Select Investor Lens → confirm tone/metrics change.
5. Create Decision → confirm saved in DB and appears in Decisions page.
6. Fork Scenario with assumption (e.g. raise_usd=5000000) → confirm new scenario saved and outputs shown.
7. Export memo → confirm markdown returns and UI displays.

========================================================
9) FINAL DELIVERABLES
=====================

* Working UI flows for decisions + scenarios
* Updated API endpoints + migrations
* Updated agent contract enforcement
* docs/copilot_v2_plan.md updated with architecture + endpoints + schemas
* Make sure TypeScript types are clean and no console errors.

If anything is missing in the current repo (e.g., no message store), implement the minimal safe version needed, but keep scope controlled.
