The current data extraction from the "Mini Benchmark Scan" PDF is mapping incorrect values to our "Truth Scan" dashboard.  Please update the parser with the following logic:

1. Use `pdfplumber` to open the PDF and search for key financial metrics.  Specifically, look for lines beginning with:
   - "In March 2025, Revenue was" → parse the dollar amount that follows (e.g. "$14.2M") and store this as `monthly_revenue`.  If the run‑rate is also present (e.g. "($171.0M run rate)"), capture it separately as `annual_run_rate`.
   - "Gross Margin was" → extract the percentage (e.g. "60.3%") and store it as `gross_margin`.
   - "Operating Margin" (or "Operating margin") → extract the percentage (e.g. "30.6%") and store it as `operating_margin`.
   - "Burn Multiple was" → extract the burn multiple (e.g. "-0.7x") and store it as `burn_multiple`.
   - "Revenue CMGR" → extract the 3‑/6‑/12‑month growth rates and choose the most recent figure (e.g. the 3‑month CMGR) for the `revenue_growth_mom` metric.

2. Do **not** multiply or scale these values beyond what the PDF reports.  For example, if the PDF states "$14.2M", store it as 14.2 (million) rather than 359.8, and if it states "60.3%", store it as 60.3 rather than 98.4.

3. If a metric is missing in the PDF, leave it blank or set it to `null` rather than defaulting to zero.  For example, if cash balance is not specified, do not set it to "$0"; instead, prompt the user to input it or mark it as unavailable.

4. When negative values are present (such as a burn multiple of –0.7×), preserve the sign and do not clamp them to zero.

5. Map the extracted values to the correct fields in the company information database:
   - `monthly_revenue` → "Monthly Revenue"
   - `operating_income` (from "Operating Income" if available) → "Monthly Surplus"
   - `gross_margin` → "Gross Margin"
   - `revenue_growth_mom` → "Revenue Growth (MoM)"
   - `burn_multiple` → "Burn Multiple"
   - `operating_margin` → "Operating Margin"

6. After updating the parser, re‑run the extraction on the AstroTalk PDF to verify that the dashboard shows:
   - Monthly Revenue ≈ $14.2 M (March 2025) with annual run‑rate ~ $171 M
   - Monthly Surplus ≈ $4.4 M
   - Gross Margin ≈ 60.3 %
   - Revenue Growth (MoM) ≈ 4.2 %
   - Burn Multiple ≈ –0.7×
   - Operating Margin ≈ 30.6 %

This should align the dashboard metrics with the actual figures in the PDF and eliminate the large discrepancies currently observed.
