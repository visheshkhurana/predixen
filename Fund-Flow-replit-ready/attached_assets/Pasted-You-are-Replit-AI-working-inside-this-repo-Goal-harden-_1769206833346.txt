You are Replit AI working inside this repo. Goal: harden the architecture, fix build/runtime footguns, and make tests runnable. Make changes directly in the codebase with minimal disruption.

CONTEXT
- Repo has a Node/Express “gateway” that serves the frontend and proxies /api to a FastAPI server.
- Node currently spawns FastAPI (uvicorn) as a child process.
- There are issues around production build, CORS + credentials, graceful shutdown, seeding/migrations running on every boot, and pytest import paths.

DELIVERABLES (must complete all)
1) Fix production build reliability (tsx/devDependencies issue)
2) Fix FastAPI CORS configuration (no wildcard origins with credentials)
3) Add graceful shutdown + liveness supervision for the uvicorn child process
4) Gate DB migrations/seeding by env flags and make them idempotent-safe
5) Make pytest pass locally by fixing pythonpath / packaging
6) Add basic health endpoints + log clarity
7) Update README with how to run in dev/prod + env vars

========================================================
1) NODE BUILD: TSX IN PROD
========================================================
Problem: package.json uses `tsx` in build scripts. In many deployments devDependencies are not installed in production, so build fails.

Do one of these (prefer A):

A) Move `tsx` to dependencies and keep scripts
- In package.json:
  - Move "tsx" from devDependencies -> dependencies
  - Keep scripts as-is

B) Or compile scripts to JS and run with node (more work; only do if A is undesirable)

Also ensure TypeScript compilation for server is deterministic:
- If server code is TS but not compiled, keep tsx as runtime dep (A).
- Add `"engines": { "node": ">=18" }` if not present.

After change:
- `npm run build` must work on a clean install where only dependencies are installed.

========================================================
2) FASTAPI CORS: SAFE + CORRECT
========================================================
Problem: allow_origins=["*"] with allow_credentials=True is invalid/unsafe.

In FastAPI (server/main.py or wherever CORSMiddleware is configured):
- Replace wildcard with env-driven allowlist:
  - Add env var: CORS_ORIGINS (comma-separated), e.g. "http://localhost:5173,https://app.example.com"
  - Parse into list; trim spaces
- If CORS_ORIGINS is empty:
  - In development, allow localhost origins by default (5173, 3000)
  - In production, default to NO origins (empty list) unless explicitly set

Implementation details:
- In server/core/config.py (or equivalent), add:
  - cors_origins: list[str] with parsing from env
  - environment: "development" | "production"
- In main FastAPI app setup:
  CORSMiddleware(
    allow_origins=config.cors_origins,
    allow_credentials=True,        # only if you need cookies/auth; keep True if used
    allow_methods=["*"],
    allow_headers=["*"],
  )

Add a note in README about setting CORS_ORIGINS.

========================================================
3) GRACEFUL SHUTDOWN + SUPERVISION FOR UVICORN CHILD
========================================================
Problem: Node spawns uvicorn but doesn’t kill it on exit and doesn’t handle crashes.

In Node server (server/index.ts or wherever `startFastAPIServer()` lives):
- Add:
  - signal handlers for SIGINT, SIGTERM, SIGHUP
  - on signal: stop HTTP server, then terminate uvicorn process, then exit
- Ensure child is spawned with stdio forwarded and a process group:
  - On *nix: spawn with `detached: true` and kill process group: process.kill(-pid)
  - On Windows: fallback to child.kill()

Add restart strategy:
- If uvicorn exits unexpectedly (exit code not 0, not caused by our shutdown):
  - log it clearly
  - optionally restart with exponential backoff (max 5 retries)
- Expose a `/health` endpoint in Node that returns:
  - ok: true
  - fastapi: "up"/"down" (based on probing FastAPI /health endpoint)
  - uptime, version (optional)

Implementation steps:
A) Add FastAPI health endpoint:
- In FastAPI app, add router:
  GET /health -> { "ok": true }

B) In Node:
- Add an async function `probeFastAPI()` that fetches `FASTAPI_URL/health` with timeout (e.g. 1s).
- Add route GET /health that returns status.

C) Graceful shutdown:
- Keep a global reference to child process and a boolean `shuttingDown`.
- On shutdown, attempt:
  - child.kill("SIGTERM")
  - wait up to 5s; if still alive, SIGKILL
  - For detached process group, kill(-pid)

========================================================
4) DB MIGRATIONS + SEEDING: ENV GATED + IDEMPOTENT
========================================================
Problem: FastAPI lifespan runs:
- Base.metadata.create_all
- run_migrations
- seed_benchmarks
- seed_demo_data
…on every boot. Risky in production.

Change behavior:
- Add env vars:
  - RUN_MIGRATIONS=true/false (default: true in dev, false in prod)
  - SEED_BENCHMARKS=true/false (default: true in dev, false in prod)
  - SEED_DEMO_DATA=true/false (default: true in dev, false in prod)
  - CREATE_SCHEMA=true/false (default: true in dev, false in prod)
- In production defaults should be false unless explicitly enabled.

Additionally:
- Ensure each seeding function is idempotent:
  - Use unique keys and check existence before insert
  - Or use upsert behavior where possible
- Ensure migrations are versioned (if using custom run_migrations):
  - Track schema_version table so migrations don’t rerun
  - If Alembic is present, use it instead (optional; only if repo already set up)
- If `Base.metadata.create_all` is used, it must not run in prod by default.

Implement:
- Update server/core/config.py with these booleans.
- In lifespan/startup:
  - if config.create_schema: Base.metadata.create_all(...)
  - if config.run_migrations: run_migrations(...)
  - if config.seed_benchmarks: seed_benchmarks(...)
  - if config.seed_demo_data: seed_demo_data(...)
- Add clear logs for what ran.

========================================================
5) PYTEST: FIX IMPORT PATH / PACKAGING
========================================================
Problem: tests fail because `server` package isn’t on PYTHONPATH.

Do one of these (prefer A):
A) Add `pytest.ini` at repo root:
  [pytest]
  pythonpath = .
  testpaths = server/tests
  addopts = -q

B) Or add `conftest.py` in server/tests that appends repo root to sys.path.

Also ensure server is treated as a package:
- Make sure `server/__init__.py` exists (create if missing).
- If there is a `pyproject.toml` / requirements, keep consistent.

After change:
- `pytest` should run from repo root successfully.

========================================================
6) HEALTH + LOGGING CLARITY
========================================================
- FastAPI:
  - Add GET /health returning ok + version optional
  - Add structured logging for startup showing config flags (without secrets)
- Node:
  - Add GET /health (as above)
  - Add logs on uvicorn spawn: command, url, pid
  - Add logs on proxy errors and uvicorn restarts

========================================================
7) README UPDATE
========================================================
Update README (or create one) with:
- Dev run:
  - npm install
  - npm run dev (Node) + FastAPI details (auto-spawn or separate)
- Prod run:
  - npm ci --omit=dev (if supported) AND confirm build still works
  - npm run build
  - npm start
- Environment variables list:
  - NODE_ENV
  - FASTAPI_PORT / FASTAPI_URL
  - CORS_ORIGINS
  - RUN_MIGRATIONS, CREATE_SCHEMA, SEED_BENCHMARKS, SEED_DEMO_DATA
  - DATABASE_URL (or whatever is used)
- Security note: do not use wildcard CORS with credentials

========================================================
IMPLEMENTATION CHECKLIST (DO THIS IN ORDER)
========================================================
1) Edit package.json: move tsx to dependencies; verify scripts
2) Add FastAPI /health endpoint
3) Fix CORSMiddleware to use env-driven origins list
4) Add config flags + gate schema/migrations/seeds
5) Add pytest.ini + ensure server/__init__.py exists
6) Update Node: graceful shutdown + restart backoff + /health probing
7) Update README
8) Run sanity checks:
  - npm run build
  - npm start (and hit /health)
  - pytest

OUTPUT REQUIREMENTS
- Provide a concise summary of changed files
- Provide the exact commands to run to verify
- If any test still fails, explain why and propose the next fix